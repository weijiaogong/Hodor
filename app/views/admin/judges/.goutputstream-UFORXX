= form_tag admin_judges_path do
    = text_field_tag :number
    = submit_tag "Generate"
= button_to "Clear", clear_admin_judges_path, method: "delete"
%table#judges
    %thead
        %th Access Code
        %th Name
        %th Company
    %tbody
        - @judges.each do |judge|
            %tr
                %td= judge.access_code
                %td= if(judge.name != ""){judge.name} else{'-'}
                %td= judge.company_name

